import React from 'react';
import { connect } from 'react-redux';

function Artist(props) {
    return (
        <div className="artist">

        </div>
    )
}

export default connect(undefined, undefined)(Artist);